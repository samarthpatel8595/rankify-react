export type OptimizationResponse = unknown;

export type SourceOption = {
  source: string;
  label: string;
};

export type OptimizationData = {
  aeo_score: number;
  gen_score: number;
  geo_score: number;
};